trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: APIContractSecurityAudit@8
  inputs:
    platformServiceConnection: 'Demolabs'
    jsonReport: 'audit-action-report-$(Build.BuildId).json'
    sarifReport: 'audit-action-report-$(Build.BuildId).sarif'


    